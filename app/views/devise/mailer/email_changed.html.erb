<p><%= t('views.users.change.title') %> <%= @email %>!</p>

<% if @resource.try(:unconfirmed_email?) %>
  <p><%= t('views.users.change.email.is_changed') %><%= @resource.unconfirmed_email %>.</p>
<% else %>
  <p><%= t('views.users.change.email.has_changed') %><%= @resource.email %>.</p>
<% end %>
