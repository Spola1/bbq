<div class="event-photo">
  <% image_title = "#{render partial: 'photos/photo_header', locals: { photo: photo }}"%>
  <%= link_to image_tag(photo.photo.url), photo.photo.url,
              data: { lightbox: 'event-photos', title: image_title, size: '40x40' } %>
</div>
