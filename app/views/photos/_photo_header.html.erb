<div>
  <%= render partial: 'users/user', object: photo.user %>
  <em><%= l photo.created_at, format: :short %></em>
  <% if current_user_can_edit?(photo) %>
    <%= button_to fa_icon('trash'),
      event_photo_path(photo.event, photo),
      form: { data: { turbo_confirm: t('photos.photo.destroy_confirm') } },
      method: :delete, class: 'bg-transparent border-0 icon-success', form_class: 'd-inline'%>
  <% end %>
</div>
