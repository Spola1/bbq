<h1><%= t('views.events.list.title') %></h1>

<div class="mb-3 mt-2">
  <%= link_to t('views.events.new.add'), new_event_path, class: 'btn btn-success' %>
</div>

<div class="row">
  <%= render partial: 'events/event', collection: @events %>
</div>
